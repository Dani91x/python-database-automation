# ─────────────────────────────────────────────────────────────────────────────
# requirements-train.txt — dipendenze PINNATE per il retrain cloud (GitHub Actions)
#
# PARITÀ DI SERVING (vincolo non negoziabile): i modelli .pkl.gz prodotti dal
# cloud vengono caricati sul PC locale dal Python di sistema (C:\Python313).
# I pickle di scikit-learn/lightgbm/xgboost sono sensibili alla versione:
# queste pin replicano ESATTAMENTE l'ambiente di serving locale (2026-06-12).
# Se aggiorni una di queste librerie sul PC, aggiorna anche questo file.
#
# NIENTE tensorflow qui: il meta-learner deve restare LogisticRegression
# ovunque, così i modelli si caricano identici su qualsiasi macchina.
# ─────────────────────────────────────────────────────────────────────────────

numpy==2.4.6
scipy==1.17.0
pandas==2.3.3
scikit-learn==1.8.0
lightgbm==4.6.0
xgboost==3.2.0
imbalanced-learn==0.14.1
optuna==4.7.0

# Infra (data fetch + upload modelli su Supabase storage/registry)
supabase==2.28.0
requests>=2.31
httpx>=0.27
python-dotenv
pytz
