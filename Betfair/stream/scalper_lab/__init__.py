"""Sandbox sperimentale scalper (loop RD-Agent). NON tocca scalper/ originale.

La logica del bot vive nella COPIA ``scalper_bot_base.py`` (identica all'originale
al momento della copia); da qui in poi viene adattata solo nel lab.
"""
from .scalper_bot_base import ScalperStrategy  # noqa: F401
