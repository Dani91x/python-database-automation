# ─────────────────────────────────────────────────────────────────────────────
# ML BETTING ENGINE — serving stack (cloud actions + PC locale)
#
# PARITÀ DI VERSIONE NON NEGOZIABILE (fix 2026-06-24):
# I modelli .pkl.gz sono picklati con versioni PINNATE (scikit-learn 1.8.0, ...).
# Caricarli con versioni diverse rompe l'unpickle (es. "'LogisticRegression' object
# has no attribute 'multi_class'") e il serving scrive ZERO predizioni.
# Per EVITARE che serving e training divergano, requirements.txt EREDITA le pin da
# requirements-train.txt (UNICA fonte di verità per le librerie version-sensitive:
# numpy/scipy/pandas/scikit-learn/lightgbm/xgboost/imbalanced-learn/optuna + infra).
# Se aggiorni una libreria, la cambi in UN SOLO posto (requirements-train.txt) e
# training + serving restano automaticamente allineati.
# ─────────────────────────────────────────────────────────────────────────────
-r requirements-train.txt

# ── Extra di SOLO serving/report (non servono al training) ───────────────────
gspread             # Google Sheets (report / money_management)
thefuzz             # fuzzy matching nomi squadre/leghe
python-Levenshtein  # backend veloce per thefuzz

# ── Stream live Betfair (Betfair/stream/*) ───────────────────────────────────
# flumine porta dietro betfairlightweight (streaming + in_play_service).
# Eseguito SOLO in locale durante le partite (vedi Betfair/PIANO_STREAM_LIVE.md).
flumine             # framework streaming/recording (sopra betfairlightweight)

# ── Opzionale / heavy (meta-learner neurale) ─────────────────────────────────
# L'ensemble degrada con grazia a LogisticRegression se assenti (try/except import).
# tensorflow
# keras
